# 사용성 점검 2026-09-19 (재설계 전 기준선)

브라우저 자동 조작(Playwright, 폰 세로/가로·320px·PC·감소된 동작)으로 찾은 문제. 심각도 높음은 재설계 구조로 해결한다.

| 심각도 | 영역 | 문제 | 재설계 반영 |
|---|---|---|---|
| 높음 | 3D · 폰 | 3D 화면 위에서 손가락으로 쓸어 넘겨도 페이지가 내려가지 않는다. 화면의 절반이 3D라 아이가 그 자리에 갇힌다. | 3D를 단계 하나로 분리해 화면 전체를 쓰고, 회전은 한 손가락, 페이지 이동은 하단 버튼으로. |
| 높음 | 3D · PC | 마우스가 3D 위에 있으면 휠로 페이지를 내릴 수 없고 확대·축소만 된다. | 휠 확대는 Ctrl+휠 또는 ± 버튼으로만. 기본 휠은 페이지 스크롤. |
| 높음 | 3D · 가로 화면 | 폰을 가로로 돌리면 3D 높이 439px가 화면 390px보다 커서 자막·재생 버튼이 화면 밖으로 나간다. | 가로 모드에서는 3D를 화면 높이에 맞추고 자막·컨트롤을 3D 위에 겹쳐 띄운다. |
| 높음 | 확인 문제 | 세 문제를 다 풀면 다시 풀 방법이 없다. 새로고침해야 한다. 틀린 아이가 재도전할 길이 막힌다. | 결과 화면에 "다시 풀기"와 "다음 정거장". 재도전은 보기 순서를 섞는다. |
| 중간 | 진도 | 자막을 눌러 마지막 장면까지 봐도 "3D 봤어요"로 기록되지 않는다. | 마지막 장면 도달 = 봤음 기록(재생 방식 무관). |
| 중간 | 3D · 폰 | 컨트롤 6개가 두 줄로 접혀 순서가 흐트러지고 속도 선택이 홀로 떨어진다. | 재생·이전·다음 3개로 줄이고 음성·속도는 설정 시트. 버튼 48px. |
| 중간 | 3D · 폰 | 자막 영역 높이가 문장 길이에 따라 72~128px로 바뀌어 아래 버튼이 장면마다 움직인다. | 고정 높이(3줄) 말풍선. 넘치면 글자 한 단계 축소. 대본은 접힘. |
| 중간 | 유닛 · 폰 | 상단 고정 영역(헤더+탭 104px)이 화면의 12%를 항상 차지하고, 탭 "⑤ 확인"은 잘려 있는데 가로 스크롤 표시가 없다. | 단계형 화면에서는 탭을 없애고 진행 점만 둔다(24px). |
| 중간 | 좁은 폰 320px | 유닛 화면이 가로로 72px 넘친다. 제목 위 정보 줄이 두 줄로 깨진다. | 정보 줄을 한 줄 라벨로. 가로 넘침 검사를 빌드 체크에. |
| 중간 | 키보드 | 개념을 읽는 중에도 ←/→ 키가 3D 장면을 넘긴다(전역 단축키). | 3D 단계가 화면에 있을 때만 단축키. |
| 낮음 | 3D | 장면 로딩 중 캔버스가 빈 크림색이고 "불러오는 중…" 글자만 있다. | docssam "실험 준비 중…" 말풍선 + 진행 점. |
| 낮음 | 음성 | 음성 토글을 켜면 소리가 나지 않는 기기에서도 켜진 것처럼 보인다. | 켤 때 첫 문장을 바로 읽고, 불가하면 안내 문구. |
| 낮음 | 홈 | 진도 배지가 이모지(🎬·✎)라 뜻이 바로 안 읽히고 기기마다 모양이 다르다. | 깃발·별 도형(SVG)과 짧은 글자. |
| 낮음 | 대본 | 자막과 같은 문장이 바로 아래 대본에 한 번 더 나와 폰 화면을 길게 만든다. | 대본은 접힘이 기본. |
| 확인됨 | 접근성 | 감소된 동작 설정에서 애니메이션 없이 장면만 넘어간다. 포커스 링 보임. Space 한 번에 한 번만 토글. | 유지. |

재설계 후 같은 스크립트로 재점검할 것: 3D 위 터치/휠 스크롤, 가로 모드, 320px 넘침, 퀴즈 재도전, 진도 기록, 전역 단축키.
