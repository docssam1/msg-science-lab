# 2026-09-22 MSG CH01 수업 인수인계

## 현재 상태 — 이전 기록보다 우선

- 사용자 승인 범위: 별도 MSG 저장소에서 초과심_물리 PART I 무게 재기 CHAPTER 01 시범 수업, 학생/강의 화면 분리, 원본 충실도, 전자칠판 참여, 강의용 PPTX.
- 원본 인쇄 10~15쪽을 모두 이미지로 읽었다. src/ch01-model.js, src/ch01.js, src/ch01.css가 현재 수업이다. index.html/student.html/lecture.html이 이 코드를 사용한다.
- 8단계 학습과 원본 daily test 6문항 구현. 서답형 유지, 학생 제출 후 풀이, 교사 단일 문항 표시/개별 풀이 공개, 판서/되돌리기/지우기/전체 화면.
- 학생 첫 시도는 브라우저 localStorage `msg-ch01-daily-first-attempt-v1`에만 저장된다. 서버/외부 전송 없음. 강의 풀이 공개는 학생 기록을 만들지 않는다.
- 0~30 N 가상 모형, 과부하 거절, 영점과 힘/눈높이 분리, 진동이 멈춘 후 읽기. 실제 교구 규격/3D/실제 촬영으로 표현하지 않는다.
- 13장 PPTX: output/MSG-초과심_물리-CH01-강의용-v1.pptx. 패키지/레이아웃/폰트 정책/Artifact Tool 재가져오기 검사 통과. PowerPoint 실행 검증과 전자칠판 하드웨어 검증은 별도다.
- 기존 tests/core.test.mjs 6개와 새 tests/ch01.test.mjs 6개, 합계 12개 통과. 최신 화면 회귀 및 최종 PPTX 전수 시각 검수 결과는 최종 검증 절에 기록한다.
- 기존 캐릭터 시연은 character.html에 보존했다. 수업에 실제 음성은 아직 연결하지 않았다.
- 사용자가 제공한 로컬 음성 원본 `녹음 2026-09-22 172855.mp4`를 확인했으므로 아래 과거 기록의 "음성 파일 요청"은 해소되었다. 11.818646초 AAC 48 kHz stereo, 정상 디코딩 확인까지다. 청취/전사/OmniVoice 음성 생성은 완료되지 않았다.
- 사용자 요청으로 에이전트 병렬 검수를 진행했다. 광고 형식은 기능 체험으로 연결되는 별도 소개 페이지로 확정했다. 미구현 기능/성과를 홍보하지 않는다.
- Git 원격 생성/커밋/푸시/외부 배포는 하지 않았다. 모든 변경과 기존 미추적 파일을 보존한다.

## 다음 작업의 보호 규칙

계획을 보고하고 승인된 범위만 변경한다. 새 챕터/음성 모델 다운로드/공개 배포는 기존 CH01 검수와 구분한다. 원본 PDF와 다른 제품 저장소를 수정하지 않는다. 실제 교실 검증 전 "전자칠판 현장 검증 완료"라고 쓰지 않는다.

## 독립 교차검토에서 보완한 점

- 표시자/막대/고리/추가 함께 4 px/N 이동하게 수정했다. 기존의 서로 다른 이동량과 줄어드는 연결 막대는 제거했다.
- 본 저울의 7 px 두께 표시자는 중심 208.5로, 시차 모형의 8 px 두께 표시자는 중심 284로 배치해 각각 윗부분 205/280이 눈금 기준이 되도록 맞췄다.
- 1920×1080/1365×900 전자칠판 화면의 하단 진행 버튼은 고정된 수업 화면 안에 유지하고, 긴 읽기/문제 내용은 본문 내부에서 스크롤한다.
- 별도 에이전트가 인쇄 10~15쪽 여섯 장과 일일평가 지문/선택지/서답형/기호 매핑을 전수 대조했다. 공식 daily 해설과 대조한 것이 아니라 본문과 기하 관계로 독립 확인한 것이다.
- 최종 PPTX를 다시 가져와 13장 전부 재렌더링/직접 보기 검수 통과. 최종 SHA-256: 68dd63f22d52032c8495b2d1eacb02f5c0877583860302e09a5d857714a192de. 교사용 정답이 발표자 노트에 있으므로 편집 가능한 강의용 파일을 학생용 자료처럼 배포하지 않는다.

## 이전 캐릭터 시범 기록 — 역사적 상태

아래 기록은 CH01 원본 수업 구현 전 상태다. 현재 수업의 완료 범위나 테스트 명령으로 사용하지 않는다.

## 완료한 범위

- 별도 로컬 Git 저장소 E:/Codex/work/msg-science-lab 생성. 기존 lete-on 파일 수정 없음.
- 사용자 승인 만화체와 표정 시안 보존, 정면 기본 포즈 추가.
- 캐릭터 확대 시연 / 학생 자습 / 강의 웹 화면을 각각 분리.
- 입 4종(닫힘/반 열림/크게/둥근), 깜빡임, 질문 눈썹, 칭찬 눈웃음, 듣기 표정.
- 무음 타임라인 시연과 실제 로컬 음성 파일의 음량 기반 근사 립싱크를 구분.
- 재생/일시정지/종료/파일 오류/문서 숨김에 따른 정지 처리. 모션 축소 설정 지원.
- 강의 화면의 정답 공개/숨김 및 다음 장 이동 시 초기화. 학생용 힌트/창작 예시 문항.

## 검증

- node --test tests/core.test.mjs: 6개 통과.
- tests/browser.mjs: 실제 Edge에서 11개 검사 통과, 페이지 오류 0, 음성 업로드 0.
- 1365px, 390px, 320px 가로 넘침 없음. 별도 캐릭터 화면도 390px 검증.
- 발화/무음 감지 검증에는 테스트용 합성 톤만 사용했다. 실제 선생님 음성으로 검증한 것이 아니다.
- 입/눈 확대, 모바일 전체 화면, 강의 화면, 인쇄 CSS 렌더를 확인했다.
- 결과: output/msg-character-motion-preview.mp4 (약 20초, 무음), output/character-preview.png.

## 남은 작업과 차단 지점

- 실제 목소리: 사용자 최신 링크 https://www.youtube.com/watch?v=e9NR2QUjMps 의 8분부터.
- 8:00~8:10 추출 명령은 YouTube 미디어 HTTP 403으로 실패. 로그의 임시 CDN URL/서명은 문서나 소스에 저장하지 않음. 브라우저 쿠키/인증 우회는 시도하지 않음.
- 사용자에게 8:00~8:10 MP3/WAV 또는 로컬 원본 파일 경로 요청함. 실제 소리의 청취/전사/복제 완료를 주장하지 말 것.
- OmniVoice 설치/모델 다운로드/음성 생성 미실행. 기존 프로젝트의 공유 설치/업로드 스크립트 미실행. 새 독립 환경에서 GPU 여유 메모리를 다시 확인한 뒤 진행.
- 현재 립싱크는 음량 기반 근사다. 정확한 음소 정렬이나 입술 영상 합성은 아니다.
- GitHub 원격 저장소/커밋/푸시/배포 미실행. PPTX와 완성 교재도 미제작.
- 현재 용수철 수업은 UI·캐릭터 테스트용 창작 모형이다. 원본 교재 공식 정답 대조가 끝난 완성 수업이라고 표현하지 말 것.

## 실행

node server.mjs (127.0.0.1:4317)

- /character.html
- /student.html
- /lecture.html

자동 브라우저 검증은 MSG_PLAYWRIGHT_URL 환경변수로 제공된 Playwright 모듈을 사용할 수 있다. 기록/개인 자료는 .proofs/와 .private/에 두고 Git에서 제외한다.
